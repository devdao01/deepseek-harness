name: PTC Mode
description: All the capabilities of Standard Mode, with tools presented through the Code Mode SDK so the model composes multi-step operations as a single TypeScript program.
order: 2
disabled: true
